========模块
    import math
    import gmpy2 as gp
    from math import sqrt 
    from math import sqrt,log,exp 
    from math import * 
    dir(math) 
    help(math)
    如果想知道python中有哪些函数，只需按两下Tab键。在调入numpy模块的前提下，输入“numpy.”然后按两下Tab键，就会出现所有numpy下的函数。

========this模块
      运行 import this 将会出现一首优美的诗文。

========math模块

========cmath模块
        与 math 的函数相同，只是改在复数下计算。





========time
    import time
    star=time.clock()
    ***
    print(time.clock()-star)
