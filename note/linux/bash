 type命令可以显示命令的类型
$ type ls

增加变量内容
$PATH=${PATH}:/home/bin
$name=${name}wang

进入内核模块
$ cd /lib/modules/`uname -r`/kernel
$ cd /lib/modules/$(uname -r)/kernel
在一串命令中，在`之内的命令会被先执行，而其执行出来的结果将作为外部的输入信息。


